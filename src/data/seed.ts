/**
 * DEPRECATED: Seed data is no longer used.
 * All data is now loaded from Supabase via useLoadData hook.
 * This file kept for reference only.
 */

import { Agent, Task, Project, Message, ScheduledTask, Memory } from '@/types';

// Empty arrays - data comes from Supabase now
export const seedAgents: Agent[] = [];
export const seedTasks: Task[] = [];
export const seedProjects: Project[] = [];
export const seedMessages: Message[] = [];
export const seedScheduledTasks: ScheduledTask[] = [];
export const seedMemories: Memory[] = [];
